# AGENTS.md

The instructions for this repo live in [CLAUDE.md](CLAUDE.md). Read that.

This file is a pointer so that an agent looking for either filename finds the same guidance. Keep the content in `CLAUDE.md` and leave this as one line, rather than letting the two drift into disagreeing about the same repo.
